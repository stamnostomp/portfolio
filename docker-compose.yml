services:
  portfolio:
    build: .
    ports:
      - "8080:80"
    restart: unless-stopped
    depends_on:
      - leaderboard

  leaderboard:
    build: ./backend
    restart: unless-stopped
    volumes:
      - leaderboard-data:/data

volumes:
  leaderboard-data:
